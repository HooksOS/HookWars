{
  "name": "@hookwars/hookos-launch",
  "version": "0.1.0",
  "private": true,
  "description": "Launches the $BULLET token on Base via HookOS (@hookos/sdk).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "launch": "tsx src/launch.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hookos/sdk": "github:HooksOS/sdk",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
